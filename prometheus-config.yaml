apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring   # troque se seu Prometheus não está nesse namespace
data:
  prometheus.yml: |-
    global:
      scrape_interval: 5s
    scrape_configs:
      - job_name: "fin-control-cluster"
        static_configs:
          - targets: ["52.188.26.21:8080"]
